# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-sysele-nxp-wrynose"
BBFILE_PATTERN_meta-sysele-nxp-wrynose = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-sysele-nxp-wrynose = "9"

LAYERDEPENDS_meta-sysele-nxp-wrynose = "core"
LAYERSERIES_COMPAT_meta-sysele-nxp-wrynose = "wrynose"

#################################################################################################################
## Intel Realsense
PACKAGECONFIG:append:pn-librealsense2 = " rsusb"

## Enable SE05: comment it to use plug-and-trust-ecc and openssl-provider-se05 of meta-imx 
#ENABLE_SE05 = "1"
